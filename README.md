
# gera-json
A small and simple Gera library for parsing and generating JSON, written in Gera and therefore supporting any compilation target.

This library should mostly be complete, but can only be fully tested after the remaining bugs in the compiler have been fixed.